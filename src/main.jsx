import React from 'react'
import ReactDOM from 'react-dom/client'
import WBCMatrix from './WBCMatrix'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WBCMatrix />
  </React.StrictMode>
)
